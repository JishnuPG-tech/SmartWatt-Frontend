import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.smartwatt.app",
  appName: "SmartWatt",
  webDir: "out",
  bundledWebRuntime: false,
};

export default config;
